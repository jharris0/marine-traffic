# selectShipTypeUI <- function(id, label = "Ship Type Dropdown") {
#   ns <- NS(id)
#   tagList(
#     dropdown_input(
#       input_id = "ship_type",
#       choices = sort(unique(ships()$ship_type))
#       )
#   )
# }
